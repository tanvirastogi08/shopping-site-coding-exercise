# shopping-site-coding-exercise

Install dependencies
- `npm install`

## Development server

To develop locally run `npm start`.

## Build

Run `npm build` to build the project. The build artifacts will be stored in the `dist/` directory.